/*
 * CenteredParagraph
 *
 * A paragraph, but centered.
 */

import styled from 'styled-components';

const CenteredParagraph = styled.div`
  text-align: center;
`;

export default CenteredParagraph;
